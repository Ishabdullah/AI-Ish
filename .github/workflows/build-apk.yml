name: Build AI-Ish APK (Production)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:  # Allow manual trigger

jobs:
  build:
    name: Build Production APK (S24 Ultra Optimized)
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: ☕ Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: 🔧 Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: 🧠 Clone llama.cpp and whisper.cpp for native AI
        run: |
          echo "🧠 Cloning llama.cpp for LLM inference..."
          cd app/src/main/cpp

          # Clone llama.cpp (lightweight depth=1 clone)
          if [ -d "llama.cpp" ]; then
            echo "⚠️ llama.cpp already exists, removing..."
            rm -rf llama.cpp
          fi
          git clone --depth 1 https://github.com/ggerganov/llama.cpp.git
          echo "✅ llama.cpp cloned ($(du -sh llama.cpp | cut -f1))"

          # Clone whisper.cpp (lightweight depth=1 clone)
          if [ -d "whisper.cpp" ]; then
            echo "⚠️ whisper.cpp already exists, removing..."
            rm -rf whisper.cpp
          fi
          echo "🎤 Cloning whisper.cpp for speech recognition..."
          # Pin to v1.7.1 for compatibility with current llama.cpp/ggml (v1.7.6 has GGML_KQ_MASK_PAD issues)
          git clone --depth 1 --branch v1.7.1 https://github.com/ggerganov/whisper.cpp.git
          echo "✅ whisper.cpp v1.7.1 cloned ($(du -sh whisper.cpp | cut -f1))"

          # Clean up .git directories to reduce size
          rm -rf llama.cpp/.git whisper.cpp/.git
          echo "🧹 Cleaned up .git directories"

          cd ../../../..

          echo ""
          echo "✅ Native AI libraries ready!"
          echo "📊 Total size: $(du -sh app/src/main/cpp/llama.cpp app/src/main/cpp/whisper.cpp | awk '{s+=$1} END {print s "M"}')"

      - name: 🔐 Create debug keystore for signing
        run: |
          mkdir -p ~/.android
          keytool -genkey -v -keystore ~/.android/debug.keystore \
            -storepass android -alias androiddebugkey \
            -keypass android -keyalg RSA -keysize 2048 -validity 10000 \
            -dname "CN=Android Debug,O=Android,C=US"
          echo "✅ Debug keystore created for APK signing"

      - name: 📦 Cache Gradle packages
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: 🏗️ Build Debug APK
        run: ./gradlew assembleDebug --stacktrace --no-daemon

      - name: 🚀 Build Release APK (Production)
        run: ./gradlew assembleRelease --stacktrace --no-daemon

      - name: 📊 Get APK Info
        id: apk_info
        run: |
          DEBUG_APK=$(find app/build/outputs/apk/debug -name "*.apk" | head -n 1)
          RELEASE_APK=$(find app/build/outputs/apk/release -name "*.apk" | head -n 1)
          
          DEBUG_SIZE=$(du -h "$DEBUG_APK" | cut -f1)
          RELEASE_SIZE=$(du -h "$RELEASE_APK" | cut -f1)
          
          echo "debug_size=$DEBUG_SIZE" >> $GITHUB_OUTPUT
          echo "release_size=$RELEASE_SIZE" >> $GITHUB_OUTPUT
          
          COMMIT_SHORT=$(git rev-parse --short HEAD)
          echo "commit_short=$COMMIT_SHORT" >> $GITHUB_OUTPUT
          
          echo "📦 Debug APK: $DEBUG_SIZE"
          echo "🚀 Release APK: $RELEASE_SIZE"

      - name: 📤 Upload Debug APK
        uses: actions/upload-artifact@v4
        with:
          name: ai-ish-debug-${{ steps.apk_info.outputs.commit_short }}
          path: app/build/outputs/apk/debug/*.apk
          retention-days: 7

      - name: 📤 Upload Release APK (Production)
        uses: actions/upload-artifact@v4
        with:
          name: ai-ish-production-${{ steps.apk_info.outputs.commit_short }}
          path: app/build/outputs/apk/release/*.apk
          retention-days: 30

      - name: 📋 Build Summary
        run: |
          echo "## 🎉 Build Complete!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📦 APK Information" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Build Type | Size | Optimization |" >> $GITHUB_STEP_SUMMARY
          echo "|------------|------|--------------|" >> $GITHUB_STEP_SUMMARY
          echo "| **Debug** | ${{ steps.apk_info.outputs.debug_size }} | Development build |" >> $GITHUB_STEP_SUMMARY
          echo "| **Release** | ${{ steps.apk_info.outputs.release_size }} | 🚀 S24 Ultra NPU optimized |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🎯 Production Features" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ NPU (Hexagon v81): Mistral-7B prefill + MobileNet-v3" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ CPU (Cores 0-3): Mistral-7B decode + BGE embeddings" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ INT8 quantization across all models" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Concurrent execution: All 3 models in parallel" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📊 Performance Targets (S24 Ultra)" >> $GITHUB_STEP_SUMMARY
          echo "- LLM: 25-35 tokens/sec (NPU + CPU)" >> $GITHUB_STEP_SUMMARY
          echo "- Vision: ~60 FPS (NPU)" >> $GITHUB_STEP_SUMMARY
          echo "- Embeddings: ~500 emb/s (CPU)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📥 Download APKs" >> $GITHUB_STEP_SUMMARY
          echo "Go to the **Artifacts** section above to download the APKs." >> $GITHUB_STEP_SUMMARY

  release:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 📥 Download Release APK
        uses: actions/download-artifact@v4

      - name: 🎉 Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: ./**/*.apk
          generate_release_notes: true
          body: |
            ## AI-Ish Production Release
            
            **Optimized for Samsung Galaxy S24 Ultra**
            
            ### 🚀 Production Features
            - NPU (Hexagon v81): Mistral-7B prefill + MobileNet-v3 vision
            - CPU (Cores 0-3): Mistral-7B decode + BGE embeddings
            - INT8 quantization for maximum performance
            - Concurrent execution: All 3 models run in parallel
            
            ### 📊 Performance (S24 Ultra)
            - LLM: 25-35 tokens/sec
            - Vision: ~60 FPS
            - Embeddings: ~500 emb/s
            - Memory: ~4.5GB total
            
            ### 📥 Installation
            1. Download ai-ish-production-*.apk
            2. Install on Samsung S24 Ultra
            3. Allow installation from unknown sources if prompted
            4. Grant required permissions
            5. Download production models from in-app dashboard
            
            ### 🔒 Privacy
            - 100% on-device processing
            - Zero telemetry
            - No internet required (except optional knowledge fetching)
            
            **Copyright © 2025 Ismail Abdullah. All rights reserved.**
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
